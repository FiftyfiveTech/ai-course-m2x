# Reference transcript — mtg-001 (211s – 331s of the internal product scoping call)
#
# THE REFERENCE FOR THIS MEETING IS NOT IN THIS REPOSITORY.
#
# It lives at `eval/snippets/mtg-001.local.txt`, which is git-ignored — same rule, and
# the same reason, as `eval/vocab.local.txt`.
#
# Why. This window is an internal engineering discussion of client delivery work: module
# names, per-tenant configuration, and five colleague first names. This repository is
# public. A vocabulary file can route those terms to a local sibling and keep working; a
# verbatim reference transcript cannot be redacted at all, because redacting it destroys
# the word-for-word correspondence that is the entire point of a WER reference.
#
# So the numbers ship and the words do not. Every `mtg-001` WER cell in
# `docs/design/day2-matrix.md` was measured against the local file, and a fresh clone
# cannot reproduce those two cells. That is a real reproducibility gap, recorded here
# rather than hidden by an empty template — see `docs/phase1-comparison.md`.
#
# Reproduce the clip (the audio is git-ignored too, per the corpus rules):
#   ffmpeg -ss 211 -t 120 -i data/raw/mtg-001-fe-uiux.wav -c:a pcm_s16le \
#       data/clips/snippet-mtg-001-2min.wav
#
# Then score against the local reference:
#   uv run python eval/wer.py --reference eval/snippets/mtg-001.local.txt \
#       --hypothesis data/comparison/large-v3-auto/mtg-001.json --window 211 331
#
# Status: reference held locally — see eval/snippets/mtg-001.local.txt
